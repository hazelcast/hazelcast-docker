name: Build PR

on:
  pull_request:

jobs:
  prepare:
    runs-on: ubuntu-latest
    name: Prepare environment
    steps:
      - name: Checkout Code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Test scripts
        run: |
          .github/scripts/test_scripts.sh
